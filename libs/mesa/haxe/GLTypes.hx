package haxe;

typedef GL = mesa.GL;

typedef Buffer = mesa.GL.Buffer;
typedef Texture = mesa.GL.Texture;
typedef Renderbuffer = mesa.GL.Renderbuffer;
typedef Query = mesa.GL.Query;

typedef Uniform = mesa.GL.Uniform;
typedef Program = mesa.GL.Program;
typedef Shader = mesa.GL.Shader;
typedef Framebuffer = mesa.GL.Framebuffer;
typedef VertexArray = mesa.GL.VertexArray;

typedef ShaderCompiler = hxsl.GlslOut;
